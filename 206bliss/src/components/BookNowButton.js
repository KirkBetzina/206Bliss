const BookNowButton = () =>{ 
    // write a function that would when the book a trip now button is clicked it routes the user to the booking form.
    // get the id of the button
    // get the route to the booking form from the booking form.       
    return(
        <div id='buttonDiv'>
            <button id='button' onclick='book'>Book A Trip Now!</button>
        </div>
    )
}

export default BookNowButton